import { ViewContainerRef, ComponentFactoryResolver } from '@angular/core';
import { ISpinnerConfig } from './ngx-spinner.types';
export declare class SpinnerAnimationDirective {
    private viewContainerRef;
    private componentFactoryResolver;
    constructor(viewContainerRef: ViewContainerRef, componentFactoryResolver: ComponentFactoryResolver);
    config: ISpinnerConfig;
    private init(conf);
}
