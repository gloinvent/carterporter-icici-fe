import { InfoDialogDirective } from './info-dialog.directive';

describe('InfoDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoDialogDirective();
    expect(directive).toBeTruthy();
  });
});
